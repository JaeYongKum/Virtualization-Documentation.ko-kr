---
title: Azure 가상 네트워크에 있는 리소스와 직접 통신 하는 중첩 된 Vm 구성
description: 중첩된 가상화
keywords: windows 10, hyper-v가 설치, Azure
author: mrajess
ms.date: 12/10/2018
ms.topic: article
ms.prod: windows-10-hyperv
ms.service: windows-10-hyperv
ms.assetid: 1ecb85a6-d938-4c30-a29b-d18bd007ba08
ms.openlocfilehash: 18ab4d1d87c22f70fe09aae5222a7d125ac9c974
ms.sourcegitcommit: 914e0dd1168daf1d2b0f22bd011035016cc08baf
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 02/23/2019
ms.locfileid: "9099391"
---
# <a name="configure-nested-vms-to-communicate-with-resources-in-an-azure-virtual-network"></a>Azure 가상 네트워크에 있는 리소스와 통신 하는 중첩 된 Vm 구성

배포 하 고 Azure 내에 중첩 된 가상 컴퓨터 구성에 대 한 원래 지침 NAT 스위치를 통해 이러한 Vm에 액세스 하는 필요 합니다. 이 인해 몇 가지 제한이 있습니다.

1. 중첩 된 Vm 온-프레미스 리소스에 액세스할 수 없는 또는 Azure 가상 네트워크 내에서 합니다.
2. 온-프레미스 리소스 또는 리소스 Azure 내에서 여러 게스트 동일한 포트를 공유할 수 없는 NAT를 통해 중첩 된 Vm 액세스할만 수 있습니다.

이 문서의 나머지 양식이 RRAS, 사용자 정의 경로 전용 "부동" 주소 공간 및 게스트 인터넷 액세스를 허용 하도록 아웃 바운드 NAT 서브넷 중첩 Vm 동작 하 고 다른 가상 컴퓨터와 같은 통신을 허용를 사용 하 여 배포 하는 방법을 안내합니다 Azure 내 VNet에 직접 배포합니다.

이 가이드를 하세요 시작 하기 전에:

1. [여기에 제공 된 지침에](https://docs.microsoft.com/en-us/azure/virtual-machines/windows/nested-virtualization) 중첩 된 가상화를 읽습니다.
2. 구현 하기 전에이 전체 문서를 참조 합니다.

## <a name="high-level-overview-of-what-were-doing-and-why"></a>수행 중인 작업의 높은 수준의 개요 및 이유
* 두 개의 nic 중첩 가능한 VM으로 만듭니다. 
* NIC를 사용 하 여 NAT와 다른 NIC를 통해 인터넷 액세스는 중첩 된 Vm을 제공 하는 하나의 하이퍼바이저를 외부 리소스에는 내부 스위치의 트래픽을 라우트하도록 사용 됩니다. 각 NIC 해야 다른 라우팅 도메인에 있을 다른 서브넷을 의미 합니다.
* 즉, 최소 세 개의 서브넷에서 사용 하 여 가상 네트워크 해야 합니다. NAT에 대 한, LAN 라우팅에 대 한 및 하나를 사용 하지 않으면는 있지만 우리의 중첩 된 Vm에 대 한 "예약". 이 문서에는 서브넷에 대 한 사용 하 여 이름 인 "NAT", "하이퍼-V-LAN" 및 "Ghosted".
* 이러한 서브넷의 크기 재량에 이지만 일부 고려 사항도 있습니다. "Ghosted" 서브넷의 크기는 Ip 결정 중첩 된 Vm에 대해 사용할 수 있습니다. "NAT" 및 "하이퍼-V-LAN" 서브넷의 크기는 Ip를 결정 하는 또한 하이퍼바이저에 대 한 해야 합니다. 따라서 하나 또는 두 개의 하이퍼바이저에만 계획 된 경우 여기에 실제로 작은 서브넷에 만들 수 기술적.
* 배경: 중첩 된 Vm은 DHCP에서에서 받지 내부 또는 외부 스위치를 구성 하는 경우에 해당 호스트에 연결 되어 있는 VNet. 
  * 즉, Hyper-v 호스트 DHCP를 제공 해야 합니다.
* Hyper-v 호스트의 VNet에 현재 할당 된 임대 인식 하지 않으므로는 호스트에에서 게 할당 된 IP 이미 존재 하는 상황을 방지 하기 위해 Hyper-v 호스트에서 방금 사용할 Ip 블록 할당 해야 합니다. 이렇게 하면 중복 IP 시나리오를 방지할 수 있습니다.
  * 블록을 선택 하는 Ip 서브넷에 있는 Hyper-v가 동일한 VNet 내에 해당 됩니다.
  * 있는 기존 서브넷에 해당 하는 이유는 ExpressRoute 위로 다시 BGP 광고를 처리 하는 것입니다. 사용 하 여 Hyper-v 호스트에 대 한 IP 범위를 방금 만든 것 이면 일련의 클라이언트가 수 있도록 정적 경로 만들려고 했습니다 온-프레미스 중첩 된 가상 컴퓨터와 통신할 수 있습니다. 이 의미는 중첩 된 Vm에 대 한 IP 범위를 구성 하 고 다음 클라이언트 해당 범위에 대 한 Hyper-v 호스트에 지시 하는 데 필요한 모든 경로 만들 수 하드 요구 사항이 아닙니다.
* Hyper-v 내에서 내부 스위치를 만듭니다 하 고 새로 생성된 된 인터페이스 DHCP에 대 한 제외 설정 범위 내에서 IP 주소가 할당은 다음 합니다. 이 IP 주소는 중첩 된 Vm에 대 한 기본 게이트웨이 될 되며 될 경로 내부 스위치와이 VNet에 연결 된 호스트의 NIC 사이 하는 데 사용 합니다.
* 호스트 라우터도 전환 하는 호스트에서 라우팅 및 원격 액세스 역할을 설치 하겠습니다.  이것은 호스트 외부 리소스와는 중첩 된 Vm 간의 통신을 허용 하는 데 필요 합니다.
* 다른 리소스 이러한 중첩 된 Vm에 액세스 하는 방법을 지시 합니다. 이 작업에 중첩 된 Vm에 있는 IP 범위에 대 한 정적 경로 포함 하는 사용자 정의 경로 테이블을 만드는 것입니다. Hyper-v에 대 한 정적 경로이 IP 주소를 가리킵니다.
* 온-프레미스에서 오는 클라이언트는 중첩 된 Vm에 도달 하는 방법을 알 수 있도록이 UDR 게이트웨이 서브넷에 배치 다음 됩니다.
* 또한 중첩 된 Vm에 대 한 연결을 요구 하는 Azure 내에서 다른 서브넷에이 UDR을 배치 됩니다.
* 여러 Hyper-v 호스트에 대 한 추가 "부동" 서브넷 만들고는 UDR에 추가 정적 경로 추가 합니다.
* Hyper-v 호스트 역할을 해제할 때는 삭제/재활용 우리의 "부동" 서브넷 및 우리의 UDR에서 정적 경로 제거 하거나 이것이 마지막 Hyper-v 호스트의 UDR을 모두 제거 합니다.

## <a name="creating-the-host"></a>호스트 만들기

여기서 I 최대 VM 이름, 리소스 그룹 등과 같은 선호 하는 구성 값 다루지 됩니다.

1. Portal.azure.com로 이동
2. 왼쪽 상단에서 "리소스 만들기"를 클릭 합니다.
3. 인기 있는 열에서 "창 서버 2016 VM"을 선택 합니다.
4. "기본" 탭에서 선택 해야 중첩 된 가상화 수 있는 VM 크기
5. "네트워킹" 탭으로 이동
6. 다음 구성을 사용 하 여 새 가상 네트워크 만들기
    * VNet 주소 공간: 10.0.0.0/22
    * 서브넷 1
        * 이름: NAT
        * 주소 공간: 10.0.0.0/24
    * 서브넷 2
        * 이름: 하이퍼-V-LAN
        * 주소 공간: 10.0.1.0/24
    * 서브넷 3
        * 이름: 희미하게
        * 주소 공간: 10.0.2.0/24
    * 서브넷 4
        * 이름: Azure Vm
        * 주소 공간: 10.0.3.0/24
7. VM에 대 한 NAT 서브넷 선택 했는지 확인
8. 이동 "검토 + 만들기" "만들기"를 선택 합니다.

## <a name="create-the-second-network-interface"></a>두 번째 네트워크 인터페이스 만들기
1. VM 후 완료 Azure Portal 내에서 찾고 프로 비전
2. VM 중지
3. 한 번 중지 "네트워킹" 설정에 따라 이동
4. "네트워크 인터페이스 첨부 하세요."
5. "네트워크 인터페이스 만들기"
6. 이름을 지정 (하지 이름과 관계, 중요 하지만 기억 하는 것을 잊지 마세요.)
7. 서브넷에 대 한 "하이퍼-V-LAN" 선택
8. 호스트에 있는 동일한 리소스 그룹 선택
9. "만들기"
10. 이 이전 화면으로 다시 이동할 경우, 새로 만든된 네트워크 인터페이스를 선택 하 고 "확인"을 선택 해야 합니다.
11. "개요" 창으로 돌아가서 이전 작업이 완료 되 면 VM을 다시 시작
12. 이동 방금 만든 두 번째 nic를 찾을 수 있습니다 이전에 선택한 리소스 그룹
13. "IP 구성" 및 "Enabled" "IP 전달" 토글을 이동한 다음 변경 사항을 저장합니다

## <a name="setting-up-hyper-v"></a>Hyper-v 설정
1. 원격 호스트에
2. 관리자 권한 PowerShell 프롬프트를 엽니다.
3. 다음 명령을 실행 합니다. `Install-WindowsFeature -Name Hyper-V -IncludeManagementTools -Restart`
4. 이렇게 호스트를 다시 시작 됩니다.
5. 설정의 나머지 부분을 계속 하는 호스트에 다시 연결

## <a name="creating-our-virtual-switch"></a>우리의 가상 스위치 만들기

1. 관리 모드에서 PowerShell을 엽니다.
2. 내부 스위치를 만듭니다. `New-VMSwitch -Name "NestedSwitch" -SwitchType Internal`
3. 새로 만든된 인터페이스는 IP를 할당 합니다. `New-NetIPAddress –IPAddress 10.0.2.1 -PrefixLength 24 -InterfaceAlias "vEthernet (NestedSwitch)"`

## <a name="install-and-configure-dhcp"></a>설치 및 DHCP를 구성 합니다.

*많은 사람들이 중첩 된 가상화 사용 먼저 시도 하는 경우이 구성 요소를 누락. 달리에서 온-프레미스에 게스트 Vm 호스트에 상주 하는 네트워크에서 DHCP를 받게 됩니다 Azure에서 중첩 된 Vm 제공 해야 합니다 DHCP은 통해 호스트에서 실행 합니다. 하거나 하는 정적 IP 주소를 할당 중첩 된 모든 VM에 상관 없는 확장 가능 해야 합니다.*

1. DHCP 역할을 설치 합니다. `Install-WindowsFeature DHCP -IncludeManagementTools`
2. DHCP 범위를 만듭니다. `Add-DhcpServerV4Scope -Name "Nested VMs" -StartRange 10.0.2.2 -EndRange 10.0.2.254 -SubnetMask 255.255.255.0`
3. 범위에 대 한 DNS 및 기본 게이트웨이 옵션을 구성 합니다. `Set-DhcpServerV4OptionValue -DnsServer 168.63.129.16 -Router 10.0.2.1`
    * 이름 확인 하려는 경우 올바른 DNS 서버를 입력 해야 합니다. 이 경우 [Azure의 재귀적 DNS를](https://docs.microsoft.com/en-us/azure/virtual-network/virtual-networks-name-resolution-for-vms-and-role-instances)사용 하 고 있습니다.

## <a name="installing-remote-access"></a>원격 액세스 설치

1. 서버 관리자를 열고 "추가 역할 및 기능"를 선택 합니다.
2. "서버 역할"에 도달할 때까지 "다음"를 선택 합니다.
3. "원격 액세스"를 확인 하 고 "역할 서비스"에 도달할 때까지 "다음"을 클릭 합니다.
4. "라우팅" 확인 "기능 추가"를 선택한 다음 "다음" 한 다음 "설치"를 선택 합니다. 마법사를 완료 하 고 설치를 완료 될 때까지 기다립니다.

## <a name="configuring-remote-access"></a>원격 액세스 구성

1. 서버 관리자를 열고 "도구"를 선택 하 고 "라우팅 및 원격 액세스"를 선택 합니다.
2. 라우팅 및 원격 액세스 관리 패널의 오른쪽에서 아이콘 옆에 있는 서버 이름으로 보고,이 마우스 오른쪽 단추로 클릭 하 됩니다 "구성 및 사용 라우팅 및 원격 액세스"를 선택 합니다.
3. 마법사에서 선택 "다음", "사용자 지정 구성"에 대 한 방사형 단추를 선택 하 고 "다음"를 선택 합니다.
4. "NAT" 확인 "LAN 라우팅" 한 다음 선택 "다음"와 다음 ""입니다. 서비스를 시작 하 고 이렇게 묻는 메시지가 나타납니다 하는 경우.
5. 이제 "IPv4" 노드로 이동 하 고 "NAT" 노드를 사용할 수 있도록 확장 합니다.
6. 마우스 오른쪽 단추로 클릭 "NAT", "새 인터페이스..." 선택 "Ethernet" 선택 "10.0.0.4"의 IP가 있는 첫 번째 NIC 이어야 함
7. 두 번째 NIC LAN 트래픽이 강제로 일부 정적 경로 만들어야 한다는 이제 "IPv4"에서 "정적 경로" 노드로 이동 하 여이 작업을 수행 합니다.
8. 일단 있습니다 다음 경로 만들어 보겠습니다.
    * 경로 1
        * 인터페이스: 이더넷
        * 대상: 10.0.0.0
        * 네트워크 마스크: 255.255.255.0
        * 게이트웨이: 10.0.0.1
        * 메트릭: 256
        * 참고: 놓습니다 여기에서이 기본 NIC가 자체 인터페이스를 향하는 트래픽을 빠르게 응답할 수 있도록 합니다. 여기서 없으면이 다음 경로 NIC 1 NIC 2 꺼질을 위한 트래픽을 하면 됩니다. 이 비대칭 경로 만듭니다. 10.0.0.1 Azure NAT 서브넷에 할당 하는 IP 주소가입니다. Azure는 범위에 대 한 기본 게이트웨이 첫 번째 사용할 수 있는 IP 사용합니다. 따라서 192.168.0.0/24 NAT 서브넷에 대 한 사용 하는 것을 게이트웨이 192.168.0.1 것입니다. Wins, 즉이 경로 대체 라우팅 더욱 구체적인 경로에 경로 아래 합니다.

    * 경로 2
        * 인터페이스: 이더넷 2
        * 대상: 10.0.0.0
        * 네트워크 마스크: 255.255.252.0
        * 게이트웨이: 10.0.1.1
        * 메트릭: 256
        * 참고: 모든 우리의 Azure VNet 하려는 트래픽에 대 한 경로 catch입니다. 두 번째 NIC 트래픽이 켜면 중첩 된 Vm에 액세스 하려면 원하는 다른 범위에 대 한 경로 추가 해야 합니다. 그럴 경우 온-프레미스 네트워크 172.16.0.0/22 다음 다른 경로 하이퍼바이저의 두 번째 NIC 해당 트래픽이 보내려고 할 합니다.

## <a name="creating-a-route-table-within-azure"></a>Azure 내 경로 테이블 만들기

깊이 읽기를 만들고 Azure 내에서 경로 관리에 대 한 자세한 내용은 [이 문서](https://docs.microsoft.com/en-us/azure/virtual-network/tutorial-create-route-table-portal) 참조 하세요.

1. 이동 https://portal.azure.com.
2. 왼쪽 위 모서리에 있는 "리소스를 만들고"를 선택 합니다.
3. 검색 필드에 "경로 테이블"를 입력 하 고 적중 횟수를 입력 합니다.
4. 상위 결과 경로 테이블,이 항목을 선택 및 "만들기"를 선택 합니다.
5. 경로 테이블 이름, 여기서에서 이름을 지정한 것 "경로-에 대 한-중첩-Vm".
6. Hyper-v 호스트에 있는 동일한 구독을 선택 했는지 확인 합니다.
7. 새 리소스 그룹 만들기는 기존 선택 및 Hyper-v 호스트에 있는 동일한 지역에서 경로 테이블을 만들면 지역 되는지 확인 합니다.
8. "만들기"를 선택 합니다.

## <a name="configuring-the-route-table"></a>경로 테이블 구성

1. 방금 만든 경로 테이블으로 이동 합니다. 포털의 상단에서 검색 창에서 경로 테이블의 이름을 검색 하 여 설정할 수 있습니다.
2. 선택 하면 경로 테이블에서 이동 하려면 "경로" 블레이드 내 합니다.
3. "추가"를 선택 합니다.
4. 이름을 지정 하 여 경로, "중첩 Vm"으로 진행 합니다.
5. 주소에 대 한 접두사는 "부동" 서브넷에 대 한 IP 범위를 입력합니다. 이 경우 10.0.2.0/24 것입니다.
6. "가상 어플라이언스"를 선택 하 고 IP를 입력 한 다음 "다음 홉 유형"에 대 한 Hyper-v에 대 한 주소 10.0.1.4, 수 고 "확인"을 선택 하는 두 번째 NIC를 호스트 합니다.
7. 이제에서 블레이드 선택 "서브넷" 내 됩니다 "경로" 바로 아래입니다.
8. "연결" 다음 우리의 "중첩 재미 있는" VNet 및 "Azure Vm" 서브넷을 선택 선택한 다음 "확인"을 선택 합니다.
9. Hyper-v 호스트에 있는 중첩 된 Vm에 액세스 해야 하는 다른 서브넷: 서브넷에 대 한이 동일한 프로세스를 수행 합니다. 연결 된 경우 

# <a name="end-state-configuration-reference"></a>최종 상태 구성 참조
이 가이드에서는 환경에는 아래 구성 합니다. 이 섹션에 대 한 참조로 사용할 수 있도록 inteded 됩니다.

1. Azure 가상 네트워크 정보입니다.
    * VNet 높은 수준의 구성 합니다.
        * 이름: 중첩 재미
        * 주소 공간: 10.0.0.0/22
        * 참고:이 구성 될 4 개의 서브넷입니다. 또한 이러한 범위에에서 설정 되지 않습니다. 원하는 환경 주소를 해도 됩니다. 

    * 첫 번째 서브넷 높은 수준의 구성 합니다.
        * 이름: NAT
        * 주소 공간: 10.0.0.0/24
        * 참고:이 여기서는 Hyper-v 호스트 기본 NIC 상주 합니다. 중첩 된 Vm에 대 한 아웃 바운드 NAT 처리에 사용 됩니다. 중첩 된 Vm에 대 한 인터넷에 게이트웨이 됩니다.

    * 두 번째 서브넷 높은 수준의 구성 합니다.
        * 이름: 하이퍼-V-LAN
        * 주소 공간: 10.0.1.0/24
        * 참고: Hyper-v 호스트 중첩 된 Vm 및 외부 Hyper-v 호스트에 인터넷이 아닌 리소스 간에 라우팅 처리 하는 두 번째 NIC 생깁니다.

    * 세 번째 서브넷 높은 수준의 구성 합니다.
        * 이름: 희미하게
        * 주소 공간: 10.0.2.0/24
        * 참고: "부동" 서브넷이 됩니다. 주소 공간은 중첩 된 Vm에서 사용 하는 및 온-프레미스에 다시 경로 알림을 처리할 수 있습니다. 실제로 없음 Vm이이 서브넷에 배포 됩니다.

    * 네 번째 서브넷 높은 수준의 구성 합니다.
        * 이름: Azure Vm
        * 주소 공간: 10.0.3.0/24
        * 참고: 서브넷 Azure Vm을 포함 합니다.

1. Hyper-v 호스트에는 아래 NIC 구성 합니다.
    * 기본 NIC 
        * IP 주소: 10.0.0.4
        * 서브넷 마스크: 255.255.255.0
        * 기본 게이트웨이: 10.0.0.1
        * DHCP에 대 한 DNS: 구성
        * IP 전달을 사용: 아니요

    * 보조 NIC
        * IP 주소: 10.0.1.4
        * 서브넷 마스크: 255.255.255.0
        * 기본 게이트웨이: 빈
        * DHCP에 대 한 DNS: 구성
        * IP 전달을 사용: 예

    * Hyper-v 가상 스위치를 내부 NIC 생성
        * IP 주소: 10.0.2.1
        * 서브넷 마스크: 255.255.255.0
        * 기본 게이트웨이: 빈

3. 경로 테이블에는 단일 규칙을 갖게 됩니다.
    * 규칙 1
        * 이름: 중첩 된 Vm
        * 대상: 10.0.2.0/24
        * 다음 홉: 가상 어플라이언스-10.0.1.4

## <a name="conclusion"></a>결론

이제 Hyper-v 호스트에 가상 컴퓨터 (32 비트 VM도!) 배포와 하는 온-프레미스에서 및 Azure 내에서 액세스할 수 있어야 합니다.
